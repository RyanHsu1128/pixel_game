# Pixel Quiz Game (像素風闖關問答)

這是一個使用 **React + Vite** 開發的像素風格問答遊戲，後端結合 **Google Sheets** 與 **Google Apps Script** 進行題目管理與成績記錄。

## 🚀 快速安裝與執行

### 1. 安裝依賴
請確保電腦已安裝 Node.js，然後在終端機執行：

```bash
npm install
```

### 2. 啟動開發伺服器
```bash
npm run dev
```
啟動後，請打開瀏覽器瀏覽終端機顯示的網址（通常是 `http://localhost:5173`）。

---

## 🛠️ Google Sheets & Backend 設定指南

本專案使用 Google Sheets 作為資料庫。請依照以下步驟設定：

### 第一步：建立 Google Sheet
1. 前往 [Google Sheets](https://sheets.google.com) 建立一個新的試算表。
2. 建立兩個工作表（Tabs），名稱分別為：**`題目`** 與 **`回答`**。

#### **`題目` 工作表欄位 (Row 1)**
請嚴格依照順序填入以下標題：
| A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|
| qn_id | question | A | B | C | D | answer |

#### **`回答` 工作表欄位 (Row 1)**
請嚴格依照順序填入以下標題：
| A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|
| ID | 闖關次數 | 總分 | 最高分 | 第一次通關分數 | 花了幾次通關 | 最近遊玩時間 |

---

### 第二步：安裝後端腳本 (Google Apps Script)
1. 在試算表中，點選上方選單的 **擴充功能 (Extensions)** > **Apps Script**。
2. 將專案中 `backend/Code.gs` 檔案的內容完整複製。
3. 把內容貼上到 Apps Script 的編輯器中（覆蓋原本的 `myFunction`）。
4. 按 `Ctrl + S` 存檔。

### 第三步：部署為 Web App
這是最關鍵的一步：
1. 點擊右上角的 **部署 (Deploy)** > **新增部署 (New deployment)**。
2. 點擊左上角的齒輪圖示 > 選擇 **網頁應用程式 (Web app)**。
3. 設定如下：
   - **說明**：Pixel Game API (隨意填)
   - **執行身分 (Execute as)**：**我 (Me)** (您的 Gmail)
   - **誰可以存取 (Who has access)**：**任何人 (Anyone)** (重要！選這個前端才能存取)
4. 點擊 **部署 (Deploy)**。
5. 第一次部署會要求授權，請點擊 **授權存取** > 選擇帳號 > **進階** > **前往... (不安全)** > **允許**。
6. 部署成功後，**複製「網頁應用程式網址 (Web App URL)」**。

---

## ⚙️ 環境變數設定

1. 在專案根目錄建立或編輯 `.env` 檔案。
2. 將剛才複製的網址貼上：

```ini
VITE_GOOGLE_APPS_SCRIPT_URL=https://script.google.com/macros/s/您的ID/exec
VITE_PASS_THRESHOLD=3    # 通過門檻 (答對幾題算過關)
VITE_QUESTION_COUNT=5    # 每次隨機抽取的題數
```
3. **重啟** 開發伺服器 (`Ctrl + C` 停止後再 `npm run dev`)。

---

## 📝 測試題庫：生成式 AI 基礎知識 (可直接複製)

以下是用於測試的 10 題生成式 AI 題目，請直接複製內容貼上到 Google Sheets 的 **`題目`** 工作表中（從第 2 列開始貼上）：

| qn_id | question | A | B | C | D | answer |
|---|---|---|---|---|---|---|
| 1 | ChatGPT 中的 "GPT" 代表什麼？ | Generative Pre-trained Transformer | Google Processing Tool | General Public Text | Graphic Pixel Technology | A |
| 2 | 在生成式 AI 中，「幻覺 (Hallucination)」是指什麼？ | AI 產生恐怖的圖像 | AI 自信地陳述錯誤的資訊 | AI 拒絕回答問題 | AI 模仿人類的情感 | B |
| 3 | 下列哪一個模型主要用於「圖像生成」？ | GPT-4 | BERT | Stable Diffusion | Llama 3 | C |
| 4 | 「Token」在大型語言模型(LLM)中通常代表什麼？ | 一種虛擬貨幣 | 用戶的登入憑證 | 文本處理的基本單位(如字詞片段) | 模型的訓練次數 | C |
| 5 | 對於 LLM，「溫度 (Temperature)」參數調高會發生什麼事？ | 回答變得更有創意且隨機 | 回答變得更固定且保守 | 模型運算速度變慢 | 模型會過熱當機 | A |
| 6 | 什麼是 RAG (Retrieval-Augmented Generation)？ | 一種隨機生成演算法 | 檢索增強生成，結合外部資料庫 | 遞歸自動生成技術 | 機器人自動導航系統 | B |
| 7 | Transformer 架構的核心機制是什麼？ | 卷積神經網絡 (CNN) | 注意力機制 (Self-Attention) | 隨機森林 (Random Forest) | 增強式學習 (RL) | B |
| 8 | 下列何者是「提示工程 (Prompt Engineering)」的主要目的？ | 優化硬體散熱 | 設計能引導 AI 產出最佳結果的指令 | 訓練模型的底層權重 | 開發 AI 的前端介面 | B |
| 9 | 預訓練 (Pre-training) 的主要目的是讓模型學習什麼？ | 特定任務的標準答案 | 語言的通用結構與世界知識 | 用戶的個人偏好 | 公司的機密數據 | B |
| 10 | 什麼是「微調 (Fine-tuning)」？ | 調整螢幕亮度 | 清除模型的記憶 | 在預訓練模型上針對特定任務進行額外訓練 | 減少模型的參數數量 | C |

---

## ❓ 常見問題

**Q: 為什麼顯示 "Network Error" 或無法載入？**
A: 請檢查 `.env` 中的網址是否正確，以及 GAS 部署時是否選擇了「**任何人 (Anyone)**」可存取。

**Q: 我修改了 Google Sheet 題目，為什麼遊戲沒變？**
A: 遊戲會在每次按下「開始」時重新抓取。如果沒變，請確認您編輯的是正確的工作表。

**Q: 本地開發顯示 CORS 錯誤？**
A: 這通常是因為 GAS 部署權限沒開對，或者是瀏覽器擴充功能的干擾，嘗試用無痕視窗測試。

---

## 🚀 (進階) 自動部署到 GitHub Pages

本專案已設定好 GitHub Actions，只要推送代碼就會自動部署。但需要先設定 GitHub Secrets。

### 1. 取得設定值
請記下您 `.env` 文件中的內容，特別是 `VITE_GOOGLE_APPS_SCRIPT_URL`。

### 2. 設定 GitHub Repository
1. 進入您的 GitHub Repository 頁面。
2. 點選 **Settings** > **Secrets and variables** > **Actions**。
3. 點選 **New repository secret**：
   - Name: `VITE_GOOGLE_APPS_SCRIPT_URL`
   - Value: (貼上您的 GAS Web App 網址)
4. (選用) 如果您想調整遊戲參數，可以點選 **Variables** 頁籤 > **New repository variable**：
   - `VITE_PASS_THRESHOLD`: 例如 `3`
   - `VITE_QUESTION_COUNT`: 例如 `5`

### 3. 設定權限 (重要)
1. 點選 **Settings** > **Actions** > **General**。
2. 往下捲到 **Workflow permissions**。
3. 勾選 **Read and write permissions**。
4. 點選 **Save**。

### 4. 推送代碼
```bash
git add .
git commit -m "Configure deployment"
git push
```
推送後，請到 **Actions** 頁籤查看部署進度。完成後您的遊戲就會自動更新到 GitHub Pages！
